函数 求和(n):
    让 结果 = 0
    让 i = 1
    当 i <= n:
        结果 = 结果 + i
        i = i + 1
    返回 结果

函数 main():
    打印 求和(100)

    # 数组（边界由 Rust 运行时检查）
    让 arr = 数组(5)
    arr[0] = 10
    arr[1] = 20
    arr[2] = 30
    打印 arr[0]
    打印 arr[1] + arr[2]

    # 越界访问会被安全终止（不会内存破坏）
    打印 arr[10]

    返回 0
