函数 main():
    # 字符串重新赋值：旧字符串自动释放（无 GC、无泄漏）
    让 s = "hello"
    s = "world"
    打印 s

    # 数组重新赋值：旧数组自动释放
    让 a = 数组(3)
    a[0] = 1
    a = 数组(10)
    打印 a[0]

    # 多层作用域，每层退出自动释放
    如果 真:
        让 tmp = "内部"
        打印 tmp
    打印 "作用域已退出，tmp 已释放"

    返回 0
