导入 "lightWeb"

函数 接口(请求):
    返回 "后端接口"

路由("GET", "/api", 接口)

页面 (标题="hello" 图标=none):
    语言="zh-cn"
    主题="dark"
    CSS = "h1 { color: #ff6b35; }"
    打印 "<h1>hello</h1>"
    样式 "h1 { letter-spacing: 2px; }"
